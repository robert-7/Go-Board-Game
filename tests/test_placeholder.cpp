#include <gtest/gtest.h>
// IWYU pragma: no_include <string>

// A simple placeholder test to ensure the testing framework is set up correctly.
// This test does nothing but pass, confirming that we can build and run tests.
TEST(PlaceholderTest, SanityCheck) { SUCCEED(); }
